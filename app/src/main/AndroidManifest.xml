<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tony.greycasino">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission
        android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".splash"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.AD_MANAGER_APP"
            android:value="true" />

        <activity android:name=".MainActivity" />

        <activity android:name=".casinoabout" />
        <activity android:name=".rewardsRecyclerview" />
        <activity android:name=".gamingcasino" />
        <activity android:name=".winnerscasino" />
        <activity android:name=".hourscasino" />
        <activity android:name=".contactcasino" />
        <activity android:name=".tourismcasino" />
        <activity android:name=".rewards" />
        <activity android:name=".poker" />
        <activity android:name=".pokertournament" />
        <activity android:name=".badbeatmenu" />
        <activity android:name=".thefiedlcafe" />
        <activity android:name=".valleyrestaurant" />
        <activity android:name=".availableJobsrecycler" />
        <activity android:name=".ActivityAvailableJobs" />
        <activity android:name=".ActivityJobDetails" />

        <activity android:name=".ActivityShowPdfFiles" />
        <activity android:name=".ActivityEvents" />
        <activity android:name=".ActivityEventDetails" />




        <!--<activity android:name=".JobViewActivity" />-->
        <activity android:name=".promotions" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".aboutcasinomenu" />
        <activity android:name=".hoursofoperationmenu" />
        <activity android:name=".contactinformationmenu" />
        <activity android:name=".bingo" />
        <activity android:name=".bingowhatshappening" />
        <activity android:name=".pricingbingo" />
        <activity android:name=".promotionsrecyclerview" />
        <activity android:name=".rewardsabout" />
        <activity android:name=".careersabout" />
        <activity android:name=".newyear" />
        <activity android:name=".thevinyls" />
        <!--<activity android:name=".splash">-->

        <!--</activity>-->
    </application>

</manifest>